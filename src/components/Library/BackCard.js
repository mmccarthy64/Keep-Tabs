import React from 'react';

const BackCard = ({comments}) => {
    return (
        <div>
            this is the back
            this is the back
            this is the back
            this is the back
            this is the back
        </div>
    );
}

export default BackCard;
